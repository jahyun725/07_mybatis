spring:
  application:
    name: chap06-transaction-lecture-source

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/menudb
    username: swcamp
    password: swcamp

# mybatis config 설정
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  type-aliases-package: com.my.transaction

  # Mapper 인터페이스와 연결될 xml 파일 위치
  mapper-locations: mappers/*.xml
