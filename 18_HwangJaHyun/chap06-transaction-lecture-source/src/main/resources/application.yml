spring:
  application:
    name: chap06-transaction-lecture-source
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/menudb
    username: swcamp
    password: swcamp

  # mybatis config ??
  mybatis:
    configuration:
      map-underscore-to-camel-case: true
    type-aliases-package: com.my.transaction
    mapper-locations: classpath:mappers/*.xml #Mapper ?????? ??? xml ?? ??